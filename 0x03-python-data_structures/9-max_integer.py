#!/usr/bin/python3
def max_integer(my_list=[]):
    if my_list = "":
        return None
    big = my_list[0]
    for i in range(my_list):
        if my_list[i] > big:
            big = my_list[i]
    return big
